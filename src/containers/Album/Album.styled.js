import styled from 'styled-components';

export const StyledAlbum = styled.div`
  .back-button {
    position: fixed;
    top: 20px;
    left: 10px;
    border-radius: 50%;
    font-size: 30px;
  }
`;
